services:
  etherwake:
    build: .
    network_mode: host
    restart: unless-stopped
    environment:
      - TARGET_MAC=${TARGET_MAC}
      - LISTEN_HOST=${LISTEN_HOST}
      - LISTEN_PORT=${LISTEN_PORT}